#include <stdio.h>
#include "../includes/push_swap.h"

int main(int argc, char **argv)
{
    // int j;
    // int i;

    // j = 0;
    // i = 0;
    (void) argc;
    if (ft_pars(argv + 1))
       ft_putstr_fd("Error\n", 1);
    return (0);
}